print(__name__)     # this will print __main__ as it is run directly and not imported as a module 
import Welcomee as w   # we imported the Welcomee.py file as a module here so now __name__ will be set to the module's name which is Welcomee

